# Copy to .env.local and fill in.

# MongoDB connection string. Database name is appended in code ("get-some").
# Example: mongodb+srv://user:pass@cluster.mongodb.net
MONGODB_URI=

# Stable salt for hashing visitor IPs. Any random long string is fine.
# Changing it invalidates past unique-visitor counts.
IP_HASH_SALT=
